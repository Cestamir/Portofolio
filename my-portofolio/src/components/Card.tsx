import React from 'react'

const Card = ({}) => {
  return (
    <div id='card'>
        {/* simple card component need to change overall layout for better composition */}
    </div>
  )
}

export default Card